# Dépendances pour Streamlit Community Cloud (qui n'utilise pas `uv`).
# Liste explicite (PyPI) : pas de build du package local. L'import du package
# `satisfactory_planner` est assuré par un bootstrap sys.path en tête de app.py.
# En local on continue avec `uv sync --extra ui`.
ortools>=9.15.6755
pydantic>=2.6
streamlit>=1.40
plotly>=5.24
folium>=0.17
streamlit-folium>=0.23
